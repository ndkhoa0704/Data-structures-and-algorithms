#ifndef HASH_CPP
#define HASH_CPP

#include "hash.hpp"

using namespace std;

template <typename T>
int HASH<T>::hashing(string key)
{
}

template <typename T>
bool HASH<T>::add(string key, T data)
{
    hashing(key);
}

#endif